---
- name: Create extraction directory
  file:
    path: "/opt/vector"
    state: directory
    mode: "0755"

- name: Download Vector archive
  get_url:
    url: "https://github.com/vectordotdev/vector/releases/download/v{{ vector_version }}/vector-{{ vector_version }}-x86_64-unknown-linux-gnu.tar.gz"
    dest: "/tmp/vector-{{ vector_version }}.tar.gz"
    timeout: 60
    validate_certs: yes
  register: download_result
  retries: 3
  delay: 5
  until: download_result is not failed

- name: Extract Vector archive
  unarchive:
    src: "/tmp/vector-{{ vector_version }}.tar.gz"
    dest: "/opt/vector"
    remote_src: yes
    extra_opts: [--strip-components=2]
  when: download_result is changed

- name: Move Vector binary to bin
  copy:
    src: "/opt/vector/bin/vector"
    dest: "{{ vector_bin_path }}"
    mode: "0755"
    remote_src: true

- name: Create Vector config directory
  file:
    path: "{{ vector_config_dir }}"
    state: directory
    mode: "0755"

- name: Deploy Vector config
  template:
    src: vector.yml.j2
    dest: "{{ vector_config_dir }}/vector.yml"
    mode: "0644"

# Блок для systemd (только не в тестовом окружении)
- name: Setup systemd service (for production environments)
  block:
    - name: Create Vector systemd unit
      template:
        src: vector.service.j2
        dest: /etc/systemd/system/vector.service
        mode: "0644"
      notify: Restart vector

    - name: Reload systemd
      command: systemctl daemon-reload

    - name: Enable and start Vector
      systemd:
        name: vector
        state: started
        enabled: true
  when: not molecule_test | default(false)
  become: true

# Запуск Vector напрямую в тестовом окружении (идемпотентная версия)
- name: Check if Vector is already running
  shell: "pgrep -f '{{ vector_bin_path }}' || true"
  register: vector_pid
  changed_when: false
  when: molecule_test | default(false)

- name: Run Vector directly in test environment (if not running)
  command: "{{ vector_bin_path }} --config {{ vector_config_dir }}/vector.yml"
  async: 3600
  poll: 0
  when: 
    - molecule_test | default(false)
    - vector_pid.stdout == ""
  changed_when: true

- name: Debug - Show Vector status
  debug:
    msg: "Vector already running with PID: {{ vector_pid.stdout }}"
  when: 
    - molecule_test | default(false)
    - vector_pid.stdout != ""

# Отладочная задача (опционально)
- name: Debug - Show Vector process status
  shell: "ps aux | grep vector || true"
  register: vector_status
  changed_when: false
  when: molecule_test | default(false)

- name: Debug - Print Vector status
  debug:
    var: vector_status.stdout_lines
  when: molecule_test | default(false) and vector_status is defined
