data_dir: "{{ vector_data_dir | default('/var/lib/vector') }}"

sources:
  journal_logs:
    type: journald

sinks:
  console:
    type: console
    inputs:
      - journal_logs
    encoding:
      codec: json
